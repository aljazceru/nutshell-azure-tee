{
  "key": {
    "kid": "encfs-key",
    "kty": "RSA-HSM"
  },
  "akv_endpoint": "https://${KEY_VAULT_NAME}.vault.azure.net",
  "maa_endpoint": "https://${ATTESTATION_PROVIDER}.${LOCATION}.attest.azure.net",
  "key_derivation": {
    "salt": "${SALT_HEX}",
    "label": "encfs"
  }
}
